
  spring:
    application:
      name: tuctapi


    datasource:
      url: jdbc:mysql://localhost:3306/tuctapi?useSSL=false&serverTimezone=America/Sao_Paulo&allowPublicKeyRetrieval=true
      username: root
      password: q1w2e3r4

      driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
      open-in-view: false

      hibernate:
        ddl-auto: update

      show-sql: true

      properties:
        hibernate:
          format_sql: true

  server:
    port: 8080
